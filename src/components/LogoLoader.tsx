import React from 'react';
import { CinematicLogoTransition } from './CinematicLogoTransition';

export const LogoLoader: React.FC = () => {
  return <CinematicLogoTransition />;
};

export default LogoLoader;
